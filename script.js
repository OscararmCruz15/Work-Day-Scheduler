// Grab current day and hour from moment
// Color the blocks based on time
// Loop through the time blocks
// If it's in the past, grey, if present, red, if future green
// if (currentHour > the id of the timeblock), add the past class

// $("#9")

// Localstorage to store all the inputs from the textareas